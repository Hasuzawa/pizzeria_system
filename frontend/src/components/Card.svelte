<script lang="ts">
    export let name: string;
</script>

<!-- round corner!, when focused, glow ring, hover then card move upward a bit   -->
<div class="w-30 h-40 bg-black">
    <h1>{name}</h1>
</div>